import { describe, expect, it } from 'vitest';

describe('Smart Attendance frontend', () => {
  it('has the expected application name', () => {
    expect('Smart Attendance').toContain('Attendance');
  });
});
