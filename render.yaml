services:
  - type: web
    name: smart-attendance-backend
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: smart-attendance-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: "10000"
      - key: CORS_ORIGIN
        value: "*"

  - type: web
    name: smart-attendance-frontend
    runtime: docker
    plan: free
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    envVars:
      - key: BACKEND_URL
        value: https://smart-attendance-backend-lzmx.onrender.com

databases:
  - name: smart-attendance-db
    plan: free
    postgresMajorVersion: "16"
